import { useEffect, useRef, useState } from "react";


export const ProtectedRoutesProvider = (props) => {
    const {
		children
	} = props;
	
	//Handle route
	const {
		pathname
	} = useRouter();

}